# AppSheet AI Processor 環境変数設定
# このファイルをコピーして .env として使用してください
# cp .env.example .env

# ===========================================
# 必須環境変数
# ===========================================

# GCPプロジェクトID
PROJECT_ID=your-gcp-project-id

# 対象スプレッドシートのID
# スプレッドシートのURLから取得: https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit
SPREADSHEET_ID=your-spreadsheet-id

# 対象シート名
SHEET_NAME=Sheet1

# 主キーカラム名（行の一意識別に使用）
PK_COLUMN=RowID

# 処理対象データのカラム名
TARGET_COLUMN=TargetText

# AI処理結果を書き込むカラム名
RESULT_COLUMN=AIResult

# Webhook認証用シークレット（32文字以上のランダム文字列を推奨）
# 生成例: openssl rand -base64 32
WEBHOOK_SECRET=your-webhook-secret-at-least-32-characters

# ===========================================
# オプション環境変数
# ===========================================

# Vertex AIリージョン（デフォルト: asia-northeast1）
# VERTEX_AI_LOCATION=asia-northeast1

# 使用するGeminiモデル（デフォルト: gemini-1.5-flash）
# VERTEX_AI_MODEL=gemini-1.5-flash

# ログレベル（DEBUG, INFO, WARNING, ERROR, CRITICAL）
# LOG_LEVEL=INFO

# アプリケーションポート（デフォルト: 8080）
# PORT=8080
